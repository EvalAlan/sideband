# OPTIONAL/EXPERIMENTAL, PARTIAL EXAMPLE ONLY. Merge into the config.yaml
# generated by `docker compose --profile googlechat run --rm googlechat`.
homeserver:
  address: http://synapse:8008
  domain: localhost # must equal MATRIX_SERVER_NAME

appservice:
  address: http://googlechat:29320
  hostname: 0.0.0.0
  port: 29320
  database: sqlite:/data/googlechat.db

bridge:
  permissions:
    "localhost": user
    "@sideband:localhost": admin
